import pygame

class Character(name:str,width:int,height:int):
    
    def __init__():
        name = self.name
        width = self.width
        height = self.height
        surface = pygame.Surface((width,height))
        
    
    def stopped(surface = self.surface):
        
        pygame.blit(surface,)
        sprite = pygame.image.load("/img/sprite_parado.png")    
    
    def running(self,width = self.width, height = self.height):
        surface = pygame.Surface((width,height))
        sprite = pygame.image.load("/img/sprite_andando.png") 
        
        